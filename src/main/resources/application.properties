spring.thymeleaf.cache=false


# ===============================
# = DATA SOURCE
# ===============================

# Establecer aquí las configuraciones para la conexión de base de datos.
spring.datasource.url=jdbc:mysql://db4free.net:3306/guillermodb?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC


# Username and secret
spring.datasource.username=guillermo
spring.datasource.password=gaalpexx

# Mantenga la conexión viva si está inactiva durante un tiempo prolongado (necesario en producción)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1


# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties. * para las propiedades nativas de Hibernate (el prefijo es
# despojado antes de agregarlos al administrador de la entidad).

# Mostrar o no cada consulta sql
spring.jpa.show-sql=true

# Hibernate ddl auto (crear, crear y soltar, actualizar): con "actualizar" la base de datos
# el esquema se actualizará automáticamente de acuerdo con las entidades Java que se encuentran en
# el proyecto
spring.jpa.hibernate.ddl-auto = update

# Permite a Hibernate generar SQL optimizado para un DBMS en particular
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect


